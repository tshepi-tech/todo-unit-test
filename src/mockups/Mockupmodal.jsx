export default function MockupModal() {
  return (
    <div>
      <h2>Mockup Modal</h2>
      <p>
        The modal is created to test if the Portal can render a modal inside as
        a child prop
      </p>
    </div>
  );
}
